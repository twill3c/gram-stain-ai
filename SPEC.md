# SPEC.md — gram-stain-ai (Gram Stain AI V1.0)

<!-- scaffold template v1.30.0 から展開(2026-09-08)。以後このファイルはプロジェクトが育てる -->

## 1. 目的

公開ライセンス(CC BY 4.0)が確認できる Gram 染色細菌画像で画像分類モデルを学習し、
ONNX へ変換してブラウザ内で推論する、教育・研究用の Web アプリを作る。
GPU 推論サーバも有償 AI API も持たず、GitHub + Vercel Hobby のゼロ課金構成で公開する。

本アプリは医療機器ではなく、出力は医学的診断を意味しない。

**このプロジェクトが答える問いは「何%当たるか」ではなく、「その%は何を測っているのか」である。**
Gram 陽性/陰性の分類精度は、**測る向き(データの割り方)を変えると別の数になる**。
V1.0 は一つの数を出して終わりにせず、三つの物差しを並べて出荷する。

## 2. 素材と典拠

| 項目 | 実測値 | 出所 | 確認日 |
|---|---|---|---|
| データセット名 | Bacteria Data for Machine Vision and Digital Biology | Mendeley Data 公式 API レコード | 2026-09-08 |
| DOI | 10.17632/cvkgfzp7ck.1 | 同上 | 2026-09-08 |
| ライセンス | CC BY 4.0 (`data_licence.short_name`) | 公式 API レコードの `data_licence` フィールド | 2026-09-08 |
| 寄与者 | Mohammad (Behdad) Jamshidi, Saleh Sargolzaee, Salimeh Foorginezhad, Omid Moztarzadeh | 同上 | 2026-09-08 |
| 公開日 / 版 | 2023-09-03 / version 1 | 同上 | 2026-09-08 |
| 配布ファイル | zip 34 本 / 3,597,653,809 バイト(3.6 GB) | 公式 API のファイル一覧 | 2026-09-08 |
| うち重複 | `Lactobacillus rhamnosus-...-001-5FKFIs.zip` と `...-001.zip` が同一サイズ(88,766,780) | 同上 | 2026-09-08 |
| 記載枚数 | 2,722 RGB(うち DIBaS 由来 689 枚 / 33 種) | レコードの description | 2026-09-08 |
| 撮影 | Nikon E200 顕微鏡・100 倍対物・Gram 染色後 | 同上 | 2026-09-08 |

**記載枚数と実測枚数は別物として扱う。** 展開後に実測し、食い違えば実測を採用して SPEC に記録する。

### 2.0 実測した中身(loop_001・`ml/verify_dataset.py`)

| 項目 | 実測 |
|---|---|
| 走査した zip | 34 本 |
| フォルダ(分類群) | 33 |
| 画像 | **2,094 枚**(PNG のみ。PNG 以外のエントリは 0) |
| 一意な画像(SHA-256) | **2,028 枚** |
| バイト完全一致の重複 | 66 組(すべて 2 枚組)。余分な複製 66 枚 |
| 内訳 | zip をまたぐ重複 60 組(重複 zip 由来・すべて `Lactobacillus rhamnosus`)/ **同一 zip 内の重複 6 組** |
| 画像形式 | 全 2,094 枚が正方・RGBA |
| キャンバス幅 | 739〜1532 px(100 通り) |
| 分類群ごとの枚数 | 57〜69 枚(`Lactobacillus rhamnosus` のみ 120 枚 = 重複 zip の分) |

**配布元の記載 2,722 枚に対し、実測は 2,094 枚である(差 628 枚)。**
記載は結合前後のどちらを指すのか判別できないため、**本プロジェクトは実測値のみを使う**。
出荷物にも実測値を書き、記載値との差があることを明示する。

**同一 zip 内に 6 組のバイト完全一致がある**ことは、重複 zip を取り除いても重複が残ることを意味する。

```text
Acinetobacter baumannii/32.png    = 33.png
Bifidobacterium spp/44.png        = 55.png
Enterococcus faecium/40(1).png    = 40.png
Enterococcus faecium/24.png       = 25.png
Neisseria gonorrhoeae/57.png      = 58.png
Staphylococcus epidermidis/23.png = 24.png
```

連番の隣どうしが一致している。**番号が違うことは中身が違うことを意味しない。**
ファイル名を group ID に使う設計は、この 6 組を別物として train と test に分けてしまう。

### 2.0.1 近接ショットは「多数」ではなかった(loop_002 で測り直した)

loop_001 は、12 枚の貼り合わせを目で見た印象から、ここに
「バイト一致しない同一視野の近接ショットが多数ある」と書いていた。
loop_002 で同一分類群の全 59,539 対を測ると、**それは支持されなかった**。

| 実測 | 値 |
|---|---|
| 同一分類群の対 | 59,539 |
| 知覚ハッシュ(dhash 256bit)の距離が 40 未満の対 | **3 組**(距離 19 / 34 / 35) |
| 次に近い対 | 距離 55 |
| 対全体の距離の中央 | 128 付近(乱数と同じ) |

分布には **36〜54 の谷**があるので、閾値はその中に置く(既定 45)。谷に置いてあるため、
値を数ずらしても結果は変わらない。

**代わりに別の構造が見つかった。** 同一 (分類群, キャンバス寸法) のセルを数えると、
1225px を除く 421 セルのうち **264 セルがちょうど 3 枚**、76% が 3 の倍数だった。

ただしこれは同一視野**ではない**。セル内の dhash 距離は中央値 126 で乱数対と変わらず、
目視でも明らかに別の視野である(染色の濃さ・背景・ピントだけが揃っている)。
**同じスライドの別視野**と読むのが自然で、配布物に無い撮影セッション ID の代わりになる。

例外は 1225px で、22 分類群にまたがって 539 枚(27.4%)ある。
一枚のスライドから 55 視野という規模ではないので、これは別の書き出し経路と見られる。
**1225px の大きなセルにはスライドの手がかりが無い。**

この節は、**目で見た印象を全体の性質として書くと外れる**という記録でもある。

### 2.1 ラベルの典拠(循環の禁止)

Gram 反応(陽性/陰性)と形態(球菌/桿菌)は、**画像からではなく種名から**決まる。
ゆえに種名→Gram 反応の対応表は**外部権威**に取り、データセット自身や本プロジェクトの
モデル出力を根拠にしてはならない(G-07)。フォルダ名は候補生成にのみ使う(HC-012)。

### 2.2 細菌でないもの

`Candida albicans` は酵母(真菌)であり細菌ではない。Gram 陽性様に染まるが、
Gram 陽性菌として学習・評価に混ぜない。

**確定(loop_001 実測)。** 根拠は二つある。(1) NCBI Taxonomy taxid 5476 の Lineage が
`Eukaryota; Opisthokonta; Fungi; …` である。(2) 細菌の権威である BacDive に該当分類群が無い。
後者は前者の独立の傍証になっている。

### 2.3 権威に当たって分かったこと(loop_001 実測・2026-09-08)

いずれも**近道が成立しないことの実測**であり、SPEC §2.1 が外部権威を要求する理由そのものである。

- **門(phylum)から Gram は決められない。** `Veillonella` は Bacillota 門でありながら
  Gram 陰性(権威 15 株が全会一致)。採用した他の Bacillota はすべて陽性なので、
  門で決め打つ実装は**ここだけ静かに間違える**。テスト T-108 がこの反例の存在を検査する
- **フォルダ名の属は現行の属ではない。** `Lactobacillus` 属は 2020 年に分割された。
  データセットの 11 フォルダは現在 5 属(`Lactobacillus` / `Lacticaseibacillus` /
  `Lactiplantibacillus` / `Limosilactobacillus` / `Ligilactobacillus`)にまたがる。
  `Propionibacterium acnes` も現在は `Cutibacterium acnes` である。
  **フォルダ名を属として切ると、物差し C が意図した単位で切れない**
- **形態は Gram より合議が割れる。** Gram は 31 分類群中 29 が一致率 100% だったが、
  形態は `Acinetobacter baumannii` が桿菌 59% / 卵形 41%(coccobacillus)であり一意に決まらない
- **語の正規化が結果を変える。** 権威は形態に 5 通りの語を使う。球形・卵形・卵円形を
  球菌にまとめないと、`Veillonella`(53.3%)と `Neisseria gonorrhoeae`(87.5%)が
  一致率不足で落ち、**Stage B から Gram 陰性球菌が全部消えて 4 クラスが 3 クラスになる**。
  正規化規則は `ml/build_labels.py` に明記し、変異検査 T-111 で「規則を変えると落ちる」ことを確かめる

### 2.4 採用結果(loop_001 実測)

| 項目 | 実測 |
|---|---|
| 採用した分類群 | 32 / 33 |
| Gram 陽性 / 陰性 | 23 分類群 / 9 分類群 |
| 一致率 100% でない Gram ラベル(tier B) | `Bifidobacterium spp` 97.1% / `Fusobacterium` 87.5% |
| Stage B 対象 | 30 分類群 |
| Stage B 対象外 | `Acinetobacter baumannii`(形態一致率 58.8%)/ `Porphyromonas gingivalis`(種水準の典拠が無い) |
| 科の数 | 19。最大は Lactobacillaceae の 11 分類群 |
| Stage B 4 クラスの分類群数 | 陽性桿菌 16 / 陽性球菌 7 / 陰性桿菌 5 / **陰性球菌 2** |

`Porphyromonas gingivalis` は種水準に Gram の記録が無く、属 `Porphyromonas` の合議
(記録のある 3 株がすべて陰性・桿菌)で補っている。**種水準の典拠ではない**ことを
`labels.json` の `authority_level: "genus"` と `caveat` に残す。

## 3. 機能要求

| ID | 要求 | 優先度 |
|---|---|---|
| F-01 | Gram 陽性 / 陰性の二値分類(Stage A)をブラウザ内で実行する | must |
| F-02 | 形態(球菌 / 桿菌)分類(Stage B)。典拠で形態が一意に定まる種のみを対象とする | should |
| F-03 | 画像アップロード(JPEG/PNG/WebP・10MB 上限)とサンプル画像の実行 | must |
| F-04 | クラス別スコアをバーで表示する。閾値未満は「確信度が低い」注記を出す | must |
| F-05 | モデル / データセット情報ページ。CC BY 4.0 の帰属と加工内容を表示する | must |
| F-06 | Gram 染色の教育コンテンツ(4 工程と染め分かれの理由) | must |
| F-07 | 医療用途でない旨を全ページに明示する | must |
| F-08 | 画像をサーバへ送らない(アップロード API を持たない) | must |
| F-09 | **三つの物差しを併記する**(画像単位 / 種ホールドアウト / 属ホールドアウト) | must |
| F-10 | **非学習の色相ベースライン**を陽性対照として併記する | must |
| F-11 | Grad-CAM を学習・評価側で実装し、reports に出す(UI は V1.1) | should |
| F-12 | 測って捨てた主張を「歩き方」ページに残す | should |

### 3.1 F-09 — 三つの物差し(本プロジェクトの目玉)

同じモデル・同じ指標でも、割り方を変えると別の問いに答えることになる。

| 物差し | 割り方 | 答えている問い |
|---|---|---|
| A 画像単位 | 分類群の中で group-aware に分割 | この 32 分類群のスライドを見分けられるか |
| B 種ホールドアウト | ある分類群の全画像を test へ(leave-one-taxon-out) | 学習で見ていない**種**の Gram 反応を当てられるか |
| C 科ホールドアウト | ある科の全分類群を test へ(leave-one-family-out) | 学習で見ていない**科**の Gram 反応を当てられるか |

C を置く理由は実測にある。採用した 32 分類群のうち **11 が Lactobacillaceae 科**である。
種ホールドアウトで 1 分類群を抜いても、近縁の 10 が train に残る。
**B は「未知の種」を測っているつもりで、実際には近縁の見覚えを測りうる。**

**C の単位を「属」ではなく「科」にしたのは実測の結果である**(loop_001)。当初は属で切る
つもりだったが、権威に当たると 11 の `Lactobacillus` フォルダは現行では 5 属にまたがっており、
属で切っても `Lacticaseibacillus casei` / `paracasei` / `rhamnosus` のように同属の兄弟が残る。
**フォルダ名で切っても現行の属で切っても、近縁は残る。** 残さない単位は科だった。

V1.0 は A・B・C の三つを macro F1 で測り、95% 信頼区間つきで並べて出荷する。
**どれが高いかを事前に決めない。** 差が小さければそれも実測結果として書く。

### 3.2 F-10 — 非学習の色相ベースライン(陽性対照)

Gram 染色は、陽性が紫(crystal violet)・陰性が桃〜赤(safranin)に染まる**色の検査**である。
したがって、学習を一切しない色相統計だけの分類器が、すでにある程度当ててしまう可能性がある。
CNN の macro F1 は、この対照と並べて初めて意味を持つ。

- 色相ベースラインは train のみで閾値を決め、test には一度だけ当てる
- CNN が対照に対してどれだけ上回ったかを、A・B・C の三つの物差しすべてで報告する

### 3.3 対照の実測(loop_003・CNN より先に測った)

染まった画素(彩度上位 10%)の **R/B 比に閾値を一本**引くだけ。学習は一切しない。
評価はどの物差しでも全 1,966 枚を一度ずつ。信頼区間は group 単位のブートストラップ 2,000 回。

| 対照 | A 画像単位 | B 分類群ホールドアウト | C 科ホールドアウト |
|---|---|---|---|
| **色相(R/B 比・非学習)** | **0.9714** [0.9593, 0.9824] | **0.9524** [0.9363, 0.9673] | **0.9601** [0.9457, 0.9736] |
| キャンバス寸法(画素を見ない) | 0.5300 [0.4956, 0.5637] | 0.5375 [0.5032, 0.5706] | 0.5359 [0.5010, 0.5699] |
| 多数派クラス | 0.4171 [0.4039, 0.4292] | 同左 | 同左 |

読み取れること:

- **色の規則は三つの物差しでほとんど動かない。** 学習で見ていない分類群でも科でも同じくらい当たる。
  色は種の同一性に依らないので当然であり、**当然であることが重要**である ——
  物差しを変えて落ちるものがあれば、それは種の見覚えに頼っている印になる
- **キャンバス寸法だけでも 0.53 出る**(多数派の 0.42 より上)。画素を一切見ずにこれだけ当たる。
  撮影条件が分類群とわずかに相関している。**この分は「画像を見て当てた」の根拠から差し引く**
- 誤りは散らばらず、**染色そのものが揺れる菌に集中する**。物差し B で
  `Listeria monocytogenes` 44%(陽性だが脱色されやすい)、
  `Porphyromonas gingivalis` 37%(陰性だが染まり方が揺れる)

### 3.4 CNN に問うこと(先に決めておく)

CNN が意味を持つのは、次のどちらかが起きたときだけである。**測る前に書いておく。**

1. 三つの物差しすべてで、色の規則を**信頼区間の重なりなく**上回る
2. 全体では並んでも、**色の規則が落ちる分類群**(§3.3 の表)で明確に上回る

2 が起きれば、CNN は色以外の手がかり(形・並び・きめ)を使っていることになる。
**どちらも起きなければ、このデータで CNN は色の規則に何も足していない。そのときはそう書く。**

### 3.5 CNN の実測(loop_004)

骨格 MobileNetV3-Small(fp32 6.08 MB)。エポック予算 3(物差し A の val で決定・test は不使用)。

| | A 画像単位 | B 分類群ホールドアウト | C 科ホールドアウト |
|---|---|---|---|
| **CNN** | 0.9731 [0.9580, 0.9858] | 0.9276 [0.9085, 0.9443] | **0.6942** [0.6577, 0.7329] |
| 色相(非学習) | 0.9714 [0.9593, 0.9824] | 0.9524 [0.9363, 0.9673] | 0.9601 [0.9457, 0.9736] |
| 多数派クラス | 0.4171 | 同左 | 同左 |
| ラベル置換(G-09) | 0.4356 [0.4074, 0.4625] | — | — |

**判定(機械が §3.4 の条件に当てはめた結果 — 検査 T-235)**

- 条件 1: **不成立**。CNN は物差し B で色の規則を下回り、C では 0.6942 まで崩落する
- 条件 2: **成立**。色の規則が落ちる 3 分類群すべてで CNN の誤りが少ない
  (`Listeria monocytogenes` 44%→33%、`Porphyromonas gingivalis` 37%→35%、
  `Staphylococcus epidermidis` 14%→2%。誤り総数 60→45)

したがって結論は「何も足していない」ではない。正確には次のとおりである。

> **CNN は色以外の手がかりを確かに少し使っているが、その手がかりは学習で見た分類群に強く依存する。**
> 全体では色の規則に勝てず、未知の科ではまったく歯が立たない。

**同じモデル・同じ指標で、測る向きを変えると 0.9731 が 0.6942 になる。**
色の規則は同じ操作で 0.9714 から 0.9601 までしか動かない。
CNN が失った差は、Gram 反応の理解ではなく**分類群の見覚え**だったということである。

ラベル置換の対照は多数派クラスと区間が重なり、G-09 を通過した(学習系に漏れは無い)。

### 3.6 骨格の選定(SPEC の当初案を実測で外した)

| 骨格 | fp32 サイズ | CPU 学習速度(実測) |
|---|---|---|
| ResNet18 | 46.8 MB | 17.4 枚/秒 |
| EfficientNet-B0 | 21.2 MB | 16.4 枚/秒 |
| **MobileNetV3-Small(採用)** | **10.2 MB**(2 クラス化後 6.08 MB) | 60.1 枚/秒 |

§6.3 は ResNet18 を標準候補としていたが、**量子化する前から N-01 の配信目標 30 MB を超える**。
目標のほうを動かすのではなく骨格を変えた。

### 3.7 F-02 — Stage B(形態: 球菌 / 桿菌)の測り方(loop_011・測る前に書いた)

対象は §2.4 の 30 分類群・**1,842 枚**(球菌 9 分類群 552 枚 / 桿菌 21 分類群 1,290 枚)。
二値(球菌 / 桿菌)で測る。Gram と組み合わせた 4 クラスにしないのは、陰性球菌が 2 分類群しかなく、
物差し B・C で「見ていない陰性球菌」を作れないからである。

**物差しは Stage A の fold をそのまま使い、Stage B 対象外の画像を抜くだけにする。**
分け直さない。分け直すと、Stage A と Stage B の成績差に「割り方の違い」が混ざる。

**対照(CNN より先に測る)。** 次の四つを A・B・C の三物差しで測る。

| 対照 | 定義 | 何を差し引くためか |
|---|---|---|
| **形の規則(非学習)** | 染まった塊の細長さの中央値に閾値一本(下記) | 「形を見て当てた」の下限 |
| 色の規則を形に当てる | Stage A と同じ R/B 比に、形のラベルで閾値一本 | このデータで**形が染色の色から推せてしまう分**(Gram と形の偏り) |
| キャンバス寸法 | 画素を見ない | 撮影条件と分類群の相関 |
| 多数派クラス | 全部を桿菌と答える | 目盛りの下端 |

形の規則の定義は**画像を分類する前に固定する**(512px の切り出しを 4 枚目視して寸法だけ決めた:
球菌の直径およそ 12〜15px、大腸菌の桿菌およそ 8×30px)。

1. 512×512 の切り出しをそのまま使う(128px に縮めると球菌が 3px になり形が消える)
2. 彩度 (max−min)/max に**画像ごとの大津の閾値**を引き、それ以上を染まった画素とする。
   Stage A の「上位 10%」は使わない —— 疎な視野では染まった画素が 10% に満たず背景を拾い、
   密な視野では 10% を超えて菌体を取りこぼす。色の比は多少混ざっても平均で効くが、形は混ざると壊れる
3. 8 近傍で連結成分に分け、**面積 30〜2,000px** の成分だけを残す(ごみと、重なって融けた塊を落とす)
4. 成分ごとに画素座標の共分散の固有値比 √(λ大/λ小) を細長さとする(λ小 の下限 0.25)
5. 画像の特徴量は残った成分の細長さの**中央値**。残る成分が無い画像は特徴量なしとし、
   train の多数派クラスを答え、**その枚数を数えて報告する**
6. 閾値は train でだけ決め、細長さ > 閾値 を桿菌と予測する

**先に分かっている弱点。** 菌が密に重なった視野(例: 目視した `Listeria monocytogenes` の 1 枚)では
成分が融けて面積上限で落ちるか、融けた塊の形を測ることになる。形の規則はそこで間違えるはずで、
**間違えることは対照の欠陥ではなく、非学習の規則の限界として報告する。**

### 3.8 Stage B の CNN に問うこと(対照を測る前に書いた)

骨格・前処理・増補・エポック予算の決め方は Stage A と同じ(予算は物差し A の val で改めて決める)。
CNN が形について意味を持つのは、次のどちらかが起きたときだけである。

1. 三つの物差しすべてで、**非学習の対照のうち強いほう**(形の規則か、色の規則を形に当てたものか —— 物差しごとに macro F1 が高いほう)を、**信頼区間の重なりなく**上回る
2. 物差し B で形の規則の誤り率が 10% 以上だった分類群の**過半で誤り率が下がり、かつ合計の誤り数も減る**

判定は Stage A と同じく機械が当てはめる。**どちらも起きなければ、このデータで CNN は形について
非学習の規則に何も足していない。そのときはそう書く。**

もう一つ、成否ではなく読み方として先に決めておく: **色の規則を形に当てたものが形の規則より高ければ、
このデータの形のラベルは染色の色で大半が推せる**ということであり、CNN が形を当てても
「形を見た」根拠にはならない。この比較は物差しごとに報告する。

Stage B のモデルをブラウザへ出荷するかは、この判定を見てから次のループで決める。

### 3.9 Stage B の対照の実測(loop_011・CNN より先に測った)

コミット 05150ac(CNN を一度も回す前)。評価はどの物差しでも 1,842 枚を一度ずつ、区間は group 単位のブートストラップ 2,000 回。

| 対照 | A 画像単位 | B 分類群ホールドアウト | C 科ホールドアウト |
|---|---|---|---|
| **形の規則(非学習・色を見ない)** | **0.7797** [0.7538, 0.8049] | **0.7520** [0.7256, 0.7785] | **0.7646** [0.7379, 0.7903] |
| 色の規則を形に当てたもの | 0.6294 [0.5929, 0.6670] | 0.5772 [0.5404, 0.6138] | 0.2832 [0.2531, 0.3161] |
| キャンバス寸法(画素を見ない) | 0.4703 [0.4280, 0.5110] | 0.4325 [0.3943, 0.4704] | 0.3308 [0.2936, 0.3670] |
| 多数派クラス(桿菌) | 0.4119 [0.3979, 0.4250] | 同左 | 同左 |

読み取れること:

- **形の規則は物差しでほとんど動かない**(0.78 → 0.75 → 0.76)。Stage A の色の規則と同じ振る舞いで、
  細長さは種の同一性に依らない。同じデータで閾値を選んだ上限は 0.7856(閾値 1.835)で、train で決めた成績はその下にある
- **このデータの形のラベルは染色の色では推せない。** 色の規則は三物差しとも形の規則より低く、
  科を抜くと **0.2832 と多数派 0.4119 すら下回る**。train の科で決めた「R/B 比のどちら向きが桿菌か」が、
  見ていない科では逆になるということで、色と形の偏りは科ごとの偶然だった
- 特徴量が取れなかった画像は 3 / 1,842 枚だけ
- 形の規則の誤りは**分類群に偏る**。物差し B で誤り率 50% を超えるのは `Enterococcus faecium` 59% /
  `Fusobacterium` 56% / `Bifidobacterium spp` 56% / `Veillonella` 53% / `Enterococcus faecalis` 52% /
  `Propionibacterium acnes` 51%。連鎖球菌・双球菌は塊が融けて細長く測られ、
  多形性の桿菌(Bifidobacterium の分岐・Propionibacterium の短桿)は丸く測られる、というのが目視と整合する読みだが、**機構は測っていない**
- §3.8 条件 2 の対象(物差し B で誤り率 10% 以上)は 17 分類群になった

### 3.10 Stage B の CNN の実測と判定(loop_011)

エポック予算 14(物差し A の val で決めた。Stage A は 3)。三物差し 22 fold とラベル置換 8 fold。

| | A 画像単位 | B 分類群ホールドアウト | C 科ホールドアウト |
|---|---|---|---|
| **CNN** | **0.9981** [0.9956, 1.0000] | **0.8525** [0.8221, 0.8816] | **0.8290** [0.7963, 0.8613] |
| 形の規則(非学習) | 0.7797 [0.7538, 0.8049] | 0.7520 [0.7256, 0.7785] | 0.7646 [0.7379, 0.7903] |
| 多数派クラス | 0.4119 | 同左 | 同左 |

**判定(機械が §3.8 の条件に当てはめた結果 — 検査 T-270)**

- 条件 1: **成立**。強いほうの対照は三物差しとも形の規則で、CNN はその区間を重なりなく上回った
- 条件 2: **成立**。形の規則の誤り率が 10% 以上の 17 分類群のうち 15 で誤りが減り、誤り総数は 373 → 141

Stage A と違い、**科を抜いても崩落しない**(Gram の CNN は 0.9731 → 0.6942、形の CNN は 0.9981 → 0.8290)。

**ただし、判定の後で数えると誤りは一様ではない**(事後の分析で判定には入れない — `python -m ml.stage_b cells`・検査 T-272)。

| Gram × 形(分類群数・枚数) | CNN A | CNN B | CNN C | 形の規則 B |
|---|---|---|---|---|
| 陰性 × 桿菌(5・309) | 0% | 8% | 18% | 29% |
| **陰性 × 球菌(2・126)** | 1% | **92%** | **98%** | 27% |
| 陽性 × 桿菌(16・981) | 0% | 9% | 9% | 14% |
| 陽性 × 球菌(7・426) | 0% | 0% | 0% | 31% |

数字は誤り率。**学習で見ていない分類群・科の Gram 陰性球菌(`Veillonella`・`Neisseria gonorrhoeae`)を、CNN はほぼ全部桿菌と答える。**
同じ分類群を見ていれば(物差し A)ほぼ当てる。条件 2 の内訳でも改善しなかった 2 分類群のうち一つは `Veillonella`(53% → 100%)である。
もう一つは `Listeria monocytogenes`(38% → 76%)で、陽性の短桿菌を球菌と答えている。

読み方として言えるのはここまでで、**機構は測っていない**(loop_013 で色相については測った —— §3.13: 紫へ塗り替えても誤りは変わらず、色相は原因ではなかった)。候補は二つある。
(a) 陰性球菌は 2 分類群しかなく、片方を抜くと学習に残るのは 1 分類群なので「桃色 × 丸い」を独立に覚えられない。
(b) `Neisseria` の双球菌の対や `Veillonella` の小ささが、見た目として桿菌に近い。
切り分けは、陰性球菌の画像を紫に塗り替えて予測が変わるかで測れるが、fold のモデルを保存していないので学習し直しが要る。

したがって結論は次のとおりである。

> **形については、CNN は非学習の規則に確かに足している。** 見ていない科でも 0.83 を保つ。
> ただし足した分は、**データに少ない組み合わせ(Gram 陰性の球菌)では失われ、そこでは非学習の規則にも大きく負ける。**

**ラベル置換の対照(G-09)を引き直した記録。** 測る前の基準は「多数派クラスの区間と重なる」で、Stage B の置換 0.4963 [0.4702, 0.5212] は
多数派 0.4119 [0.3979, 0.4250] を上回り**落ちた**。置換モデルの予測を fold 内で 2,000 回入れ替えた分布は中央 0.4963・95% [0.4743, 0.5196]
(観測以上の割合 p=0.544)で、画像から何も読んでいない。多数派は全部を同じ答えにする予測の値で、ばらけて答える予測の偶然水準ではない。
Stage A の置換 0.4356 が旧基準で通っていたのは入れ替え分布より低かったからで、漏れを見分けて通ったのではない。
**基準を結果を見てから変えたので、変えたことと旧基準での結果を残す**(`reports/permutation_null.json` の `superseded_majority_criterion`)。

**Stage B を画面に出すかは、この崩れを見て次のループで決める。** 出すなら、陰性球菌で信頼できないことを結果の横に書く必要がある。

### 3.11 Stage B を画面に出す(loop_012・利用者の判断)

利用者が「形のモデルを画面に出す」と決めた。§3.10 の崩れを隠さない形で出す。

**配るもの。** Gram のモデルと配信物には触らない(別ファイルにする)。

| ファイル | 中身 |
|---|---|
| `public/models/model_shape.onnx` | 物差し A の 70/15/15 の train で学習し、test には一度だけ当てたモデル(G-08) |
| `public/models/labels_shape.json` | `["coccus", "bacillus"]`。Python 側の 球菌 = 0 / 桿菌 = 1 と同じ並び |
| `public/models/model_shape_metadata.json` | 三物差し・形の規則・ラベル置換の偶然水準・判定・`negative_cocci`(陰性球菌の誤り率と注意書き) |

**画面の約束。**

1. 形の結果は Gram の結果と**別の塊**に出す(`data-model="shape"`)。スコアは「モデル出力スコア」と呼び、診断確率と呼ばない
2. 形の結果の横に、**いつでも**次の注意を出す(`data-caveat="negative-cocci"`)。数は `model_shape_metadata.json` から読み、画面に直接書かない:
   「学習で見ていない分類群・科の Gram 陰性球菌を、このモデルはほぼ全部桿菌と答えます(科を抜いた評価で誤り NN%)」
3. Gram のモデルが陰性、形のモデルが桿菌と答えたときは、**その組み合わせこそ見ていない陰性球菌でも出る答えだ**と一文足す
4. Stage B の対象外の分類群(`Porphyromonas gingivalis` など、形の典拠が一つに決まらないもの)のサンプルでは、形の学習・評価に入っていないことを出す
5. 三つの物差しのページに Stage B の表(CNN と形の規則・Gram × 形の誤り)を、配信メタデータから出す

**ゲートは Gram と同じものを形にも当てる。** G-01(二実装照合)・G-02(実ブラウザ照合・10 サンプル)・G-08(test は一度)・N-01(二つの ONNX の合計 30 MB 以内・2 枚目以降の推論 2 秒以内 —— 1 回の判定で二つのモデルを走らせる)。

### 3.12 陰性球菌で崩れる理由を測る(loop_013・測る前に書いた)

§3.10 の崩れ(見ていない Gram 陰性球菌を B 92% / C 98% 外す)の機構の候補は二つだった。
**(a) 色の近道** —— 学習に残る陰性球菌が 1 分類群しかなく、「桃色なら桿菌」に寄っている。
**(b) 見た目そのもの** —— `Neisseria` の双球菌の対や `Veillonella` の小ささが桿菌に近く見える。
これを、**形はそのままに色だけを変える操作**で切り分ける。

**モデル。** 陰性球菌の分類群が試験側に入った fold だけを学習し直し、**重みを保存する**(以前の sweep は予測しか残していない)。
物差し B の fold 0(`Neisseria` を抜く)・fold 2(`Veillonella` を抜く)、物差し C の fold 0(`Neisseria`)・fold 3(`Veillonella` と `Pseudomonas aeruginosa`)。
手続き・エポック・乱数の種は loop_011 の sweep と同じ(fold i の種は SEED + i)。

**操作(塗り替え)。** 画像の全画素を HSV にし、**色相だけを Δ 回して、彩度と明度はそのまま**戻す。
Δ = 目標の色相 − その画像の染まった画素の色相(`colour_features.json` の `hue_deg`)。
目標は、Stage B の Gram 陽性画像の `hue_deg` の**円周平均**(紫)、逆向きの操作では Gram 陰性画像の円周平均(桃)。
形の規則の染色マスクは彩度から作るので、**この操作は形の規則の特徴量を構造上変えない**(検査 T-283 で確かめる)。
偽の塗り替えは同じ変換を Δ = 0 で通すもの。

**確かめてから判定する(どれかが落ちたら判定しない)。**

| ID | 確かめること | 基準 |
|---|---|---|
| M1 操作が色を動かした | 紫へ塗り替えた陰性球菌を、学習しない色の規則(R/B 比・閾値 1.1999)が陽性と答える割合 | ≥ 90% |
| M2 形を変えていない | 陰性球菌 126 枚の形の規則の特徴量(細長さ・成分数)が塗り替え前後で一致 | 全件一致 |
| M3 変換そのものは中立 | 偽の塗り替えで、各モデルの陰性球菌の誤り率が変わる幅 | < 5 ポイント |

**判定(物差しごとに、その物差しの 2 fold・126 枚をまとめて)。** 誤り率の下がり幅 = 元の画像の誤り率 − 紫に塗り替えた画像の誤り率。

- **色が主因**: 下がり幅 ≥ 50 ポイント、かつ塗り替え後の誤り率 ≤ 40%
- **見た目が主因**: 下がり幅 < 20 ポイント
- **混合**: それ以外
- 下がり幅の 95% 区間は group 単位のブートストラップ 2,000 回で添える。二つの物差しで判定が割れたら、割れたと書く

**副次(判定には入れない・報告する)。**
S1 特異性: 物差し C fold 3 のモデルで、見ていない陰性桿菌(`Pseudomonas aeruginosa`)を紫にしたとき誤り率がどれだけ上がるか。
S2 逆向き: 4 つのモデルの試験側にある Gram 陽性球菌を桃に塗り替えたとき誤り率がどれだけ上がるか。
S3 再現: 学習し直したモデルの予測が、loop_011 の sweep で保存した fold の予測とどれだけ一致するか。

### 3.13 陰性球菌で崩れる理由の実測(loop_013)

`python -m ml.mechanism measure` → `reports/mechanism_negative_cocci.json`。判定規則は §3.12(e07a52c で測る前にコミット)。

**確認はすべて通った。** M1 紫へ塗り替えた陰性球菌を色の規則が陽性と答えた割合 **100%** / M2 126 枚すべてで形の規則の特徴量が一致 /
M3 偽の塗り替えで 4 モデルとも誤り率の変化 **0 ポイント**。学習し直したモデルの予測は、loop_011 の sweep で保存した予測と **4 fold とも 100% 一致**した(S3)。

| 物差し(2 fold・126 枚) | 元の誤り率 | 紫へ塗り替えた誤り率 | 下がり幅 [95% 区間] | 判定 |
|---|---|---|---|---|
| B 分類群ホールドアウト | 92.1% | 93.7% | −1.6 ポイント [−9.2, 5.8] | **見た目が主因** |
| C 科ホールドアウト | 97.6% | 96.0% | 1.6 ポイント [−2.5, 6.2] | **見た目が主因** |

分類群ごと: `Neisseria gonorrhoeae` B 85%→88%・C 96%→93%、`Veillonella` B・C とも 100%→100%。

**副次(判定には入れない)。** S1 見ていない陰性桿菌(`Pseudomonas aeruginosa`・63 枚)を紫にしても誤り 0%→0%。
S2 陽性球菌を桃にしても誤り 0%→0%(`Enterococcus faecium`・56 枚)・1.5%→0%(`Streptococcus agalactiae`・65 枚)。
**形のモデルは、どちら向きにも色相で形を決めていない。**

**言えることの範囲。** 規則の名前は「見た目が主因」だが、実際に確かめたのは**色相を回しても答えが変わらない**ことである。
回したのは色相だけで、**彩度・明度(染まりの濃さ)・菌体の大きさ・並び方(双球菌の対)は変えていない**。
したがって結論は「§3.10 の候補 (a) 色の近道は、色相については否定された。見ていない陰性球菌を桿菌と答える手がかりは色相以外の画像の性質にあるが、どれかは特定していない」までである。
学習に残る陰性球菌が 1 分類群しかないこと自体(候補 (a) の前半)が効いているかどうかも、この操作では測っていない。

## 4. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | ONNX モデル 30MB 以下(loop_012 から二つの合計)/ モデルロード 5 秒以内 / 1 画像推論 2 秒以内(一般的 PC)。**モデルロードは、本番へ配った直後の最初の取得(端の保存が冷えた状態)と、暖まった状態の両方で測り、目標はどちらにも当てる**(loop_012 で明記。配った直後に 5,097 ms、暖まってから 1,759 / 1,944 ms を測った —— 再実行して通ったことは、冷えた状態で最初に来る利用者が待たないことの証拠にならない・HC-245) | 実ブラウザで実測し README に記録 |
| N-02 | ゼロ課金構成(DB・認証・外部 AI API・サーバ関数なし) | 依存とルートの検査 |
| N-03 | raw データセット・権利未確認画像・利用者画像をコミットしない | .gitignore + CI 検査 |
| N-04 | ライセンス三分離(コード MIT / データ CC BY 4.0 / 学習済みモデル) | LICENSE / DATA_LICENSE.md / MODEL_LICENSE の記載 |
| N-05 | 専用 venv と固定 requirements.txt で再現可能 | 別環境での再実行 |
| N-06 | CI(lint / typecheck / unit test / Next.js build)が緑 | GitHub Actions |
| N-07 | 日本語本文にキリル文字等の混入がない | harness/text_hygiene.py |
| N-08 | フリート共通フッタ規約(5 項目・下部固定)に準拠 | 実ブラウザ検品 |

## 5. 較正ゲート

ゲートは**実装より先に**置く。落ちたゲートは、閾値を下げて通すのではなく、
落ちた事実と落ちた理由を出荷物に書く(先行プロジェクトの規律に従う)。

### 5.0 ゲートの現況(2026-09-15・loop_011 時点)

| ゲート | 状態 | 実測 |
|---|---|---|
| G-01 二実装照合 | **通過**(引き直し後・§5.1) | argmax 一致率 1.000000 / 確率の最大差 1.125e-5 |
| G-02 ブラウザ照合 | **通過**(本番でも) | 10/10・差 ≤ 0.043 ポイント |
| G-03 leakage 検査 | **通過** | 分割をまたぐ重複 0 件 |
| G-04 group split 健全性 | **通過** | またぐ group 0 件 |
| G-05 陽性対照 | **通過** | 色相ベースラインを三物差しで併記 |
| G-06 目玉 | **通過** | A/B/C を 95% 区間つきで併記 |
| G-07 ラベル典拠 | **通過** | 採用 32 分類群すべてに典拠 |
| G-08 test を一度だけ | **通過** | `test_evaluations` = 1 |
| G-09 学習系の陽性対照 | **通過**(loop_011 で引き直した基準で・Stage A / B とも) | 入れ替えの偶然水準の上側 97.5% を超えない: Stage A 0.4356(上側 0.5095)/ Stage B 0.4963(中央 0.4963・上側 0.5196・p=0.544)。**旧基準では Stage B が不通過**(多数派 0.4119 を上回る) |
| G-10 Grad-CAM 検査 | **通過**(loop_008・測る前に引き直した形で・弱い) | 集中度比 学習済み 1.379 [1.309, 1.459] 対 乱数化 0.948 [0.911, 0.989]・健全性の順位相関 -0.035 |
| G-11 非画像の陽性対照 | **通過** | キャンバス寸法だけで 0.53(多数派 0.42 より上) |
| G-12 Stage B の対照を CNN より先に | **通過**(loop_011) | 対照 4 種 × 三物差しを 05150ac でコミットしてから CNN を回した。§3.8 の二条件は機械が当てはめ、両方成立(§3.10) |

**G-10 は loop_008 で測った(11 本すべてに結果がある)。** ただし通過は弱い。

- 面積 10% の染まった画素に乗る CAM の重みは平均 13.8%。残り 86% は染まっていない画素に乗る
- 染まった画素は色と形の両方を含むので、この比では「形を見ている」と「色を見ている」を分けられない
- CNN が色の規則に最も勝った *Listeria monocytogenes*(§3.5 の条件 2)で比が 0.983 と菌体に寄っていない(9 枚・区間なし)。
  主張にはしないが、「CNN の優位は分類群の見覚えである」という読みと食い違わない

旧い G-10(「視野外の黒縁を見ていない」)は前処理が黒隅を入力から消しているため何を実装しても通るので、
**測る前に**引き直した。宣言だけで守られて見えるゲートの一種だった。

### 5.1 G-01 を引き直した記録(loop_005)

G-01 は当初「出力の max abs 差 < 1e-4 かつ argmax 一致率 100%」と書いていた。
**実測は 1.0133e-4 で、この閾値をわずかに超えた。**

閾値を下げるのではなく、**閾値の当て方が誤っていた**と判断して引き直した。理由は二つある。

- **生 logits は有界でない。** 実測の範囲は −7.263〜7.019(絶対値の中央 3.217)。
  絶対差の閾値は**モデルの確信度に比例**してしまい、同じ数値品質でも確信が強いほど大きく出る。
  尺度に依存する量に固定の閾値を引いたのが誤りだった
- **製品が表示するのは確率であって logits ではない。** 測る量が、利用者が見る量と違っていた

引き直した後の実測(全 1,966 枚):

| 量 | 実測 |
|---|---|
| argmax 一致率 | **1.000000** |
| softmax 確率の最大差 | **1.125e-5**(表示上 0.001 ポイント) |
| 生 logits の最大絶対差(参考) | 1.0133e-4 |
| 生 logits の絶対差の中央値 | 1.0e-5 |
| 最大差の実体 | logit −1.8936 対 −1.8935 → 確率 0.97457 対 0.97456 |

差の正体は float32 の累積順序の違いであり、実装の食い違いではない。
**旧い書き方での結果(1.0133e-4・不通過)も出荷物に残す。** 引き直したことを隠さない。

先例: manazashi-lab loop_001 も G-02 を生 logits から「順位 + 確率 + bit 一致」の三本へ分解している。

| ID | ゲート | 判定 |
|---|---|---|
| G-01 | ONNX parity: PyTorch と ONNX Runtime に同一入力を与え、**argmax 一致率 100%** かつ **softmax 確率の最大差 < 1e-3**(全画像)。生 logits の最大絶対差も測定値として併記する | 自動テスト |
| G-02 | ブラウザ照合: ONNX Runtime Web の出力が Python ONNX Runtime と一致(サンプル画像・許容差 1e-3) | 実ブラウザ E2E |
| G-03 | leakage 検査: 分割をまたぐ SHA-256 完全重複 0 件、かつ知覚ハッシュ近傍(距離 ≤ 閾値)0 件 | 自動テスト |
| G-04 | group split 健全性: 同一 group ID が二つの分割に現れない | 自動テスト |
| G-05 | **陽性対照**: 非学習の色相ベースラインの macro F1 を A・B・C 三物差しで測り、CNN と並記した | reports 検査 |
| G-06 | **目玉**: A・B・C の macro F1 を 95% CI つきで並記した。差の有無を実測で述べた | reports 検査 |
| G-07 | ラベル典拠: 採用した全種について Gram 反応・形態の典拠 URL と確認日が `metadata/labels.json` にあり、実際に開いて確認した | 自動テスト + 目視 |
| G-08 | test セットをモデル選択に使っていない(選択は val、test は最終一度) | 手順とログ |
| G-09 | 学習系の陽性対照: ラベルを乱数に置換したとき、macro F1 がチャンス水準へ落ちる(HC-163: 損失の比では対照にならない)。**チャンス水準は、同じ予測を fold の中で test 画像どうし入れ替えた分布で作り、観測がその上側 97.5% を超えないことで判定する**(loop_011 で引き直した。旧基準「多数派クラスの区間と重なる」は多数派が偶然水準でないため、漏れの無い Stage B を落とした —— §3.10) | 自動テスト |
| G-10 | Grad-CAM 検査(loop_008 で**測る前に**引き直した): (a) 健全性 —— 学習済みと重みを乱数化したモデルの CAM の順位相関の中央値 < 0.5 (b) 本体 —— 染まった画素(彩度上位 10%)への CAM の集中度比で、学習済みの 95% 区間の下限が乱数化モデルの区間の上限を上回る。旧文言「視野外の黒縁を見ていない」は、前処理が黒隅を入力から消しているため何を実装しても通るので廃した | 自動テスト + reports |
| G-11 | **非画像の陽性対照**: 画素を一切見ず、キャンバス寸法など撮影メタデータだけで Gram を当てる分類器を測る。これが当ててしまうなら、その分だけ「画像を見て当てた」の根拠が弱い | 自動テスト + reports |
| G-12 | **Stage B の対照を CNN より先に**: 形の規則(非学習)・色の規則を形に当てたもの・キャンバス寸法・多数派の四つを、Stage B 対象 1,842 枚について A・B・C 三物差しで測り、CNN の Stage B の成績はそのあとに §3.8 の二条件へ機械で当てはめる。形の規則は幾何だけを見て色に依らないことを合成画像で確かめる | 自動テスト + reports |

## 6. データ設計

### 6.1 分割

- 基本: Train 70% / Validation 15% / Test 15%
- **crop 単位のランダム分割は禁止。** 同一原画像から複数 crop を作る場合は原画像単位で group split
- **ファイル名を group ID に使わない。** §2.0 の実測どおり、連番の隣どうしがバイト一致する組が
  6 つある。ファイル名は「別の画像である」ことを保証しない
- group ID は次の連結成分とする(§2.0.1 の実測にもとづく)。
  1. SHA-256 の完全一致は 1 枚に畳む
  2. 同一 (分類群, キャンバス寸法) のセルは、**枚数が 12 以下ならまとめる**(スライドの代理)
  3. dhash 距離が閾値(45)以下の対はつなぐ(同一視野)
- **撮影セッション ID は配布物に無い(取れなかった)。** 上の 2 は代理であって本物ではない。
  1225px のような大きなセルは group を作れないまま残り、**その分だけ物差し A は甘い**。
  甘さの量(group を作れなかった画像の枚数と割合)を出荷物に書く
- 閾値は、**まとめすぎ / まとめ足りない**の両方向を目視で確かめてから決める。
  閾値を決めた根拠(距離の分布・見た組数・その内訳)を `reports/group_calibration/` に残す
- **一つの group が二つの分類群にまたがってはならない**(T-221)。またぐなら、まとめすぎている

### 6.2 前処理

標準入力は 224×224 RGB。

配布画像の実物は次の形をしている(loop_001 実測)。前処理はこの形から出発する。

- 正方キャンバスに**円形の視野が内接**し、四隅は黒い。アルファチャンネルがその円形マスクになっている
- キャンバスの寸法が画像ごとに違う(実測 930〜1225 px)
- 同一視野の近接ショットが混在する

したがって:

- **視野円の内接正方形(一辺 = 直径 / √2)を中心から切り出す。** そのまま resize すると
  黒い四隅が入り、黒の面積は視野円の位置と寸法で変わる。**背景の黒は分類情報ではないのに、
  撮影ごとに違う量で入ってくる**ので、モデルが拾える近道になる
- **アルファは捨てる前に使う。** `convert("RGB")` はアルファを合成せず捨てるため、
  視野の外の画素値が何であっても素通りする。円の中心と直径はアルファから測る
- **キャンバス寸法を揃える段で、菌の見かけの大きさが変わる。** 対物倍率は全画像 100 倍なので
  画素あたりの実寸は本来共通のはずだが、寸法の違う画像を一律に 224 へ縮めると
  縮小率が画像ごとに変わる。寸法が分類群と相関していれば、これは近道になる。
  **G-11 でキャンバス寸法だけの分類器を測り、相関の有無を先に確かめる**
- Resize / RandomResizedCrop、水平・垂直反転(顕微鏡視野に向きの意味がないことを確認した上で)
- **Hue / Saturation / ColorJitter を強く振らない。** Gram 染色では色調が分類情報そのもの
- Normalize は ImageNet 統計(転移学習の前提に合わせる)

### 6.3 モデル

当初は ResNet18 + ImageNet 転移学習を標準候補としていたが、
**loop_004 の実測で外した**(§3.6)。採用は **MobileNetV3-Small + ImageNet 転移学習**。
選定指標は macro F1・モデルサイズ・ブラウザ推論時間・ONNX 互換性。
精度差が小さければ配信サイズの小さい方を採る(N-01)。

## 7. スコープ外(V1.0)

- 菌種同定(Stage C)。データ品質とクラス平衡を検証してから V1.2 で扱う
- 物体検出・セグメンテーション(V2.1 / V3.0)
- Grad-CAM の Web UI 表示(V1.1)
- 実患者画像のアップロード誘導。教育用サンプルと利用者の手持ち画像に限る
- 臨床診断性能の主張。感度・特異度を診断指標として提示しない
- サーバ側推論・DB・認証・ログイン
- DIBaS 原画像の当リポジトリへの直接収録(再配布条件が未確認のため)
