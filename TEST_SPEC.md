# TEST_SPEC.md — gram-stain-ai

<!-- scaffold template v1.30.0 から展開(2026-09-08) -->

## 実行規約

- `pytest -x -q` を stage 3–5 の判定に使用。マーカー: `unit` / `integration` / `validation`
- フィクスチャ更新は専用コミット(`test: update fixtures`)で行い、理由をループログに記す
- 解析解を期待する合成フィクスチャは、期待値の導出前提(直交性・一意性・単一帰属等)を
  **テスト内の assert で検算**し、導出過程をコメントに残す。前提を検算しない期待値は
  正しい実装を落とす(VERIF-FALSE / HC-004)

## 期待値の出所(HC-016)

合成フィクスチャだけでなく、**外部データ・外部権威に対する期待値**にも同じ規律が要る。
ケースごとに、その期待値がどこから来たのかを書くこと。

| 出所 | 書き方 |
|---|---|
| SPEC の条項 | 条項 ID を書く。**SPEC の保証粒度を超える期待値を書かない**(超えるなら先に SPEC を上げる) |
| 外部権威(公表値・規格・検証データ) | 出典 URL と取得日をフィクスチャの先頭に書く |
| 実測 | **実測日と実測値**をコメントに残す。「誰かの仮定」と区別できるようにする |

件数・行数は定数で書かず、**集合の一致・取りこぼしの不在**という不変量で書く。
外部データは重複や欠落を含むため、数はデータが動くたびに壊れる。

## オラクルの出所

| フィクスチャ | 出所 | 性格 |
|---|---|---|
| `dataset/metadata/labels.json` の Gram 反応・形態 | BacDive v2 API(Leibniz Institute DSMZ・CC BY 4.0)。2026-09-08 取得 | **非循環**。ラベルは画像でもモデル出力でもなく、プロジェクトの外にある権威から来る |
| `Candida albicans` の除外 | NCBI Taxonomy taxid 5476 の Lineage(Eukaryota; Fungi) | **非循環**。細菌でないことを分類学の権威が述べている |
| `dataset/metadata/raw_manifest.json` の SHA-256 | 配布元 zip の実測 | 実測。取得物が変わったことを検出する |
| ONNX parity(G-01) | PyTorch と ONNX Runtime の**二実装照合** | **非循環**。どちらも同じ重みを別実装で走らせる。片方が壊れれば一致しない |
| 学習系の陽性対照(G-09) | ラベルを乱数に置換した走行 | 対照。信号が無い入力で成績が出たら、成績の出所は信号ではない |
| 非学習の色相ベースライン(G-05) | 画素の色相統計のみ。学習を経ない | 対照。CNN の成績はこれとの差でしか意味を持たない |

## 赤で始まっていないケースについて(loop_001)

`labels.json` を作る層(`ml/build_labels.py`)は、テストより先に書いてしまった
(ループログ loop_001 の `PROC-SKIP` に記録)。したがって T-101〜T-110 は赤から始まっていない。
代わりに **T-111(変異検査)** を置き、採用規則を壊すと検査が落ちることを確かめる。
規則を変えても緑のままなら、その検査は規則を守っていない。

## ケース一覧

**「対応要求」には SPEC の ID を必ず書く**(HC-157)。品質ゲート `G-xx` を確かめるケースには
その `G-xx` を書くこと —— **SPEC のゲート表と、それを確かめるケースの対応を機械で数えられる形に
しておくため**である。どのケースからも参照されない `G-xx` は、SPEC 側に「未実装」と明記するか、
ケースを書くかのどちらかにする。宣言しただけのゲートは、誰も守っていないのに守られて見える。

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-101 | G-07 | 採用した全分類群に Gram の典拠(authority / authority_level)がある | 欠けている分類群が 0 件 |
| T-102 | G-07 | 除外した分類群には、除外理由と**外部権威の URL** がある | `Candida albicans` に NCBI Taxonomy の URL がある |
| T-103 | G-07 | Gram の値は `positive` / `negative` のみ | 他の値が 0 件 |
| T-104 | SPEC §2.1 | 形態の語がすべて正規化表で解決されている | `unmapped_shape_terms` が空 |
| T-105 | SPEC §2.1 | `stage_b` が false の分類群には理由が書かれている | 理由が空の分類群が 0 件 |
| T-106 | F-09 | 採用した全分類群に `group_species` / `group_genus` / `group_family` がある | 欠けが 0 件(物差し B・C が切れる) |
| T-107 | F-09 | フォルダ名の属と現行の属が食い違う分類群がある(旧名が混じっている) | 1 件以上。**食い違うのが正常**であり、これを検出できない実装は物差し C を誤って切る |
| T-108 | SPEC §2.1 | 門(phylum)だけから Gram を決められない反例が存在する | Bacillota 門に陰性の分類群がある(Veillonella) |
| T-109 | N-03 | `raw_manifest.json` の全ファイルで公表サイズと実測サイズが一致する | 不一致 0 件 |
| T-110 | N-03 | 配布 zip に同一 SHA-256 の重複がある(既知) | 重複グループが 1 件以上検出されている |
| T-111 | SPEC §2.1 | **変異検査**: 形態の正規化表を空にすると、Stage B 対象の分類群が減る | 正規化なしでは Gram 陰性球菌が 0 になる |
| T-112 | N-07 | リポジトリ内の日本語本文に字種違反がない | `harness/text_hygiene.py` が違反 0 件 |
| T-113 | 全 G-xx | SPEC のゲート表の各 `G-xx` が、本ファイルのケース一覧から参照されている | 未参照の `G-xx` は SPEC 側に「未実装」と明記されている |
| T-114 | SPEC §2.0 | 配布元の記載枚数と実測枚数の両方が記録され、食い違いが SPEC に書かれている | 実測値が SPEC 本文に現れる |
| T-115 | G-03 | バイト完全一致の画像重複が検出されている(zip 間・zip 内の両方) | 重複グループが 1 件以上。余分な複製数が実測枚数と一意枚数の差に一致 |
| T-116 | G-04 | **ファイル名を group ID に使えないことを実データで示す** | 同一 zip 内でバイト一致する組が 1 件以上ある(連番の隣どうしが一致する) |

### 前処理と分割(loop_002・`tests/test_splits.py`)

このループでは**実装より先に書いて赤を確認した**(12 件すべて error)。

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-201 | G-03 | 分割をまたぐ SHA-256 完全重複が 0 件 | 同一画像が二つの分割に現れない |
| T-202 | G-03 / G-04 | 同一 group ID(知覚ハッシュ近傍でまとめた集合)が二つの分割に現れない | またぐ group が 0 件 |
| T-217 | SPEC §6.2 | 切り出しが視野円の内接正方形に収まっている | はみ出す画像が 0 枚 |
| T-218 | SPEC §6.2 | 切り出しは全画像で同じ画素寸法(画像ごとにリサイズしない) | 寸法が 1 通り |
| T-219 | G-03 | 前処理後の台帳にバイト完全一致が残っていない | 重複 0 組 |
| T-220 | G-04 | 知覚ハッシュ近傍が同じ group にまとめられている | 2 枚以上を含む group が 1 個以上 |
| T-221 | G-04 | 一つの group が二つの分類群にまたがらない | またぐ group が 0 件(まとめすぎの検出) |
| T-222 | F-09 | 物差し A では各分類群が train / val / test すべてに現れる | 欠けが 0 |
| T-223 | F-09 | 物差し B の各 fold で、test の分類群が train に現れない | 重なり 0 |
| T-224 | F-09 | 物差し C の各 fold で、test の科が train に現れない | 重なり 0 |
| T-225 | F-09 | ホールドアウトの各 fold の test に Gram 陽性と陰性の両方がある | 片方だけの fold が 0。**fold の作り方が macro F1 を壊しうるため、作る側で保証する** |
| T-226 | F-09 | 各物差しで全画像がちょうど一度ずつ使われている | 被覆が全画像と一致(三つの物差しが同じ母集団を測る) |

**`skip` は赤ではない。** 当初これらは生成物が無いとき `pytest.skip` していたが、それでは
生成器が黙って出力しなくなったときに検査が緑のまま通る。素材(`dataset/raw`)がある環境では
`pytest.fail` する形に直した。素材を持たない環境(取り立ての clone)だけが skip する。

### 陽性対照と指標(loop_003・`tests/test_baselines.py`)

こちらも**実装より先に書いて赤を確認した**(10 件すべて失敗)。

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-227 | G-05 / G-06 | 自前の macro F1 が scikit-learn と一致する | 乱数入力 50 通りで差 < 1e-12 |
| T-228 | G-05 | 多数派クラスの macro F1 が解析解と一致する | 差 < 1e-12 |
| T-229 | G-06 | 信頼区間が **group 単位**で取られている | 独立単位を 200 → 10 に減らすと区間が広がる。**極端な 1 group では確かめない**(再標本が原標本と一致して区間幅が 0 になる — VERIF-FALSE を一度踏んだ) |
| T-230 | G-06 | 95% 区間が点推定を含む | lo ≤ 点推定 ≤ hi、かつ [0, 1] に収まる |
| T-207 | G-05 | 対照の成績が三つの物差しすべてで記録されている | 色相 / 寸法 / 多数派 × A / B / C の 9 通りが揃う |
| T-211 | G-11 | キャンバス寸法の対照が**画素を一切見ていない** | 関数本体に画像を開く呼び出しも経路も無い(ソースを読んで確かめる) |
| T-231 | G-08 | 色相ベースラインの閾値が train だけで決められている | 三つの物差しの成績が、同じデータで閾値を選んだ上限を超えない |
| T-232 | F-09 | 三つの物差しが同じ母集団を評価している | 評価枚数が三つとも同じ |
| T-233 | G-05 | 色相ベースラインが多数派クラスを明確に上回る | 信頼区間が重ならない |
| T-234 | F-09 | 各物差しで全画像がちょうど一度ずつ予測されている | 予測枚数 = 全画像数 |

### CNN の評価(loop_004・`tests/test_cnn.py`)

こちらも**実装より先に書いて赤を確認した**(7 件すべて失敗)。

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-208 | G-06 | CNN の macro F1 が三つの物差しすべてで 95% 区間つきで記録されている | 9 通りが揃い、区間が点推定を含む |
| T-236 | F-09 | CNN と対照が同じ母集団で測られている | 評価枚数が一致 |
| T-206 | G-09 | **ラベルを乱数に置換すると成績が多数派クラスの水準へ落ちる** | 置換後の区間が多数派の区間と重なる。上回れば学習系に漏れがある |
| T-237 | G-08 | エポック予算が test を見ずに決められている | `chosen_on` が `ruler_a_val`。選定に使った曲線が残っている |
| T-238 | N-01 | 骨格の fp32 サイズが配信目標 30MB 以下 | 超えるなら量子化か骨格変更。目標は動かさない |
| T-235 | SPEC §3.4 | **合否が、測る前に決めた二条件から機械で出ている** | 記録された判定が、実際の信頼区間から計算した値と一致。二条件のどちらも成立しないなら結論が「何も足していない」 |
| T-239 | G-08 | 出荷モデルの学習に test が入っていない | `trained_on` が train のみ、`test_evaluations` が 1 |

T-235 の趣旨は、**判定をこちらの言葉で書かせない**ことにある。書いてよいのは条件だけで、
条件に当てはめる仕事は機械にさせる。そうしないと、数を見てから
「効いた」と言える理由を探すことになる。

### ONNX 変換と二実装照合(loop_005・`tests/test_onnx.py`)

こちらも**実装より先に書いて赤を確認した**(6 件すべて失敗)。

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-204 | G-01 | **PyTorch と ONNX Runtime の二実装照合** | argmax 一致率 100% かつ softmax 確率の最大差 < 1e-3(全 1,966 枚) |
| T-245 | SPEC §5.1 | **引き直す前のゲートの結果が出荷物に残っている** | 旧規則(生 logits の絶対差 < 1e-4)の実測値と合否、引き直した理由が記録されている |
| T-240 | N-01 | 配信する ONNX が 30MB 以下 | 超えるなら量子化か骨格変更 |
| T-241 | F-01 | 入出力の名前と形が Web 層の前提どおり | `input` / `logits`、[N,3,224,224] → [N,2]、バッチ次元が動的 |
| T-242 | N-04 | 配信物が揃い CC BY 4.0 の帰属が同梱されている | `model.onnx` / `labels.json` / `model_metadata.json` と帰属文 |
| T-243 | F-09 | **配信メタデータが三つの物差しをすべて載せている** | 出荷モデルの test の数だけを配ると、画面がその数だけを示すことになる |
| T-244 | G-08 | 出荷する ONNX が、成績を測ったときと同じ予測を返す | argmax 一致率 100%、macro F1 が記録と一致 |

T-204 の許容差は **loop_005 で引き直した**(SPEC §5.1)。生 logits の絶対差で測っていたが、
logits は有界でないため閾値がモデルの確信度に比例し、しかも**製品が表示するのは確率**である。
旧規則での実測 1.0133e-4(不通過)は T-245 が出荷物に残す。

### Web 層(loop_006・`tests/preprocess.test.ts` / `tests/bundle.test.ts` / `scripts/verify-browser.mjs`)

**この層は赤から始まっていない**(ループログ loop_006 の `PROC-SKIP`・HC-253)。
ただし**照合の期待値は実装より先に作ってある** —— `ml/build_samples.py` が Python 側の
テンソルと logits を書き出しており、テストは自分の実装を写したものではない。

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-246 | F-03 | サンプルが 1 枚以上ある | — |
| T-247 | G-02 | **ブラウザ側の前処理が Python と同じ数を作る** | 正規化済みテンソルの最大差 < 1e-5(サンプル 10 枚) |
| T-248 | F-04 | softmax が合計 1・大小関係を保つ・大きな入力で溢れない | — |
| T-249 | N-01 | 束の側に wasm が混入していない | `_next/` に `.wasm` が 0 件 |
| T-250 | N-02 | 実行系を自オリジンから名指ししている | 束に `/ort/ort.wasm.bundle.min.mjs` の文字列がある |
| T-251 | N-02 | サーバ関数を持たない | `_next/server` と `api` が無い |
| T-252 | F-07 | **全ページに医療用途でない旨が出ている** | 404 を除く全 HTML に明示がある |
| T-253 | N-08 | フッタが規約どおり 5 項目 | MIT / GitHub / 測り方 / 設計図 / App Menu |
| T-254 | N-01 | 配信物が揃い、`model.onnx` が 30MB 以下 | — |
| T-255 | F-09 | **画面に出る数が配信メタデータと同じ出どころから来ている** | メタデータの macro F1 が生成 HTML にそのまま現れる |

#### 実ブラウザ検品(`node scripts/verify-browser.mjs`)

vitest は Node の中で前処理だけを突き合わせる。PNG の復号も ONNX Runtime Web も
別の実装が担うので、そこは覆えない。**利用者と同じ経路**を Playwright に歩かせる。

| 見るもの | 判定 | 実測(2026-09-10) |
|---|---|---|
| 画面の%が Python の ONNX Runtime と一致(G-02) | 差 ≤ 0.05 ポイント | 10/10 通過(最大 0.043 pt) |
| 外部への通信(N-02 / F-08) | 0 件 | 0 件 |
| 1 枚目(モデル取得と初期化を含む・N-01) | ≤ 5000 ms | 1052 ms |
| 2 枚目以降(N-01) | ≤ 2000 ms | 最大 173 ms / 中央 149 ms |

**この検品は一度、自分の競合で偽の不通過を出した。** サンプルを押した直後は直前の得点が
まだ DOM に残っており、得点の出現だけを待つと古い値を読む。
押したサンプルの学名と得点の両方を待つ形に直し、3 回連続の通過を確認した
(ループログの `VERIF-FLAKE`)。

### Grad-CAM(loop_008・`tests/test_gradcam.py`)

**実装より先に書いて赤を確認した**(5 件すべて失敗・HC-253 を受けて順序を守った)。

G-10 は測る前に引き直した。旧文言「上位反応が視野円の外(黒隅)に無い」は、
前処理が黒隅を入力から消しているので何を実装しても通る。

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-256 | F-11 | **Grad-CAM の実装が、定義から手で計算した値と一致する** | 畳み込み 1 層 + 全域平均 + 線形の小さなネットで最大差 < 1e-6(全域平均の勾配は空間で一様なので解析的に書ける) |
| T-257 | G-10(a) | 重みを乱数化すると地図が変わる(Adebayo et al. 2018) | 順位相関の中央値 < 0.5 |
| T-258 | G-10(b) | 判定が測る前に書いた規則から機械で出ている | 学習済みの集中度比の区間下限 > 乱数化モデルの区間上限、の真偽と記録が一致 |
| T-259 | G-10 | 全面 0 の地図を黙って捨てていない | 使った枚数 + 捨てた枚数 = 全枚数 |
| T-210 | G-10 | 「染まった画素」の定義が色相ベースラインと同じ | 彩度の分位が一致 |

### CI の依存(loop_009・`tests/test_ci_deps.py`)

**直す前に赤を確認した**(T-260 が落ちる状態で書いた)。

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-260 | N-06 | CI の pip install が、検査の実行時に要る配布物をすべて入れている | tests/ の構文木(**関数の中の import を含む**)と、検査が読み込む ml モジュールのモジュール水準の import から数えた一覧が、ci.yml の pip install 行に含まれる |
| T-261 | N-06 | 走査器の陽性対照: 関数の中の import を見落とさない | 合成入力で関数内の import を拾い、モジュール水準の走査は拾わない |

**なぜ要るのか。** loop_007 で CI の依存を「実測」で絞ったとき、その計器は grep で行頭の import だけを見ていた。
loop_008 の T-256 が関数の中で `import torch` しており網を抜けた。手元の venv には torch があるので緑に見え、
リポジトリが GitHub に上がっていないので CI は一度も走っていなかった。clone した空のディレクトリで
CI と同じ依存だけを入れて再現して露見した(56 通過 1 失敗・HC-269)。

### スマホでの押しやすさ(loop_010・`scripts/verify-mobile.mjs`)

**実装より先に書き、本番に当てて赤を確認した**(ファイル選択が 253x21px)。

| 見るもの | 判定 |
|---|---|
| 操作の入口(サンプル・ファイル選択・見出しのナビ)のタップ領域 | 幅・高さとも 44px 以上(Apple Human Interface Guidelines の最小タップ領域 44pt) |
| サンプルをタップすると**押したサンプルの**結果が出る | 押したサンプルの学名と得点の両方を待つ |
| どのページも横にはみ出さない | 文書幅 ≤ 画面幅 |

フリート共通の固定フッタのリンクは規約で決まった寸法なので、数えるが合否には入れない。

**なぜ要るのか。** mobile:true の根拠を「横にはみ出さない」だけで測っていた。表示が崩れないことと指で操作できることは別の性質である(フリートの physics-puzzle-lab に前例がある)。さらに前版のタッチ検品は、二回目のタップで直前の結果を読んで通過と出していた —— loop_006 の VERIF-FLAKE と同じ型の競合を、書き直した検品にまた入れていた。

### フッタの行き先(loop_010・`tests/footer.test.ts`)

**実装より先に書き、赤 2 件を確認した**(GitHub が仮の `https://github.com/`、MIT License にリンクなし)。

| 見るもの | 判定 |
|---|---|
| MIT License の行き先 | `https://github.com/twill3c/gram-stain-ai/blob/main/LICENSE` |
| GitHub の行き先 | `https://github.com/twill3c/gram-stain-ai` |
| App Menu の行き先 | `https://app-menu-amber.vercel.app/`(`app-menu.vercel.app` は他者のサイト) |
| 5 項目の並び | MIT License → © → GitHub → 測り方 → 設計図 → App Menu の出現順 |

**なぜ要るのか。** 「どれかのリンクが github.com を向いている」では、MIT License と GitHub のどちらかが化けても通る(HC-098)。スマホ検品はフッタの寸法を数えていたが行き先は見ておらず、GitHub 公開の後も仮のリンクが本番に残っていた。フッタの行き先を外したのは loop_007(App Menu)に続き 2 回目。

### Stage B — 形(loop_011・`tests/test_stage_b.py`)

**実装より先に書いた。** 合成画像の期待値は描いた図形から決まる(円の軸比 1、8x30 の楕円の軸比 3.75)。

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-262 | G-12 | 形の規則が**形を測る** | 半径 6 の円だけの視野の細長さ < 1.3、8x30 の楕円(向きはまちまち)だけの視野 > 2.5 |
| T-263 | G-12 | 形の規則が**色に依らない** | 同じ図形を紫(crystal violet 様)と桃(safranin 様)で描いて、細長さと成分数が一致する |
| T-264 | G-12 | 面積の外れを落とす | 面積 30 未満の点と 2,000 超の塊だけの視野は成分 0・特徴量なし |
| T-265 | SPEC §3.7 | 染まった画素の閾値が疎密に追随する | 疎な視野(20 個・約 1%)でも密な視野(625 個・約 27%)でも、描いた数だけ成分を数える。**上位 10% の固定分位ではどちらかで壊れる** |
| T-266 | SPEC §3.7 | Stage B の fold は Stage A の fold から対象外を抜くだけ | 抜いた後の train / test は元の部分集合で、Stage B 対象外が一枚も無く、足された画像も無い |
| T-267 | F-09 | Stage B の各 fold の train に両クラスがある | 三物差しの全 fold で球菌・桿菌が train にいる |
| T-268 | G-12 | 対照が四種 × 三物差しで記録されている | 形の規則 / 色の規則を形に当てたもの / 寸法 / 多数派 × A・B・C が揃い、区間が点推定を含む |
| T-269 | F-09 / G-08 | Stage B 対象の全画像がちょうど一度ずつ、train で決めた閾値で予測されている | 評価枚数 = Stage B 対象枚数(1,842)。形の規則と色の規則の成績が同じデータで閾値を選んだ上限を超えない。特徴量なしの枚数が報告されている |
| T-270 | SPEC §3.8 | **Stage B の CNN の合否が、測る前に書いた二条件から機械で出ている** | 記録された判定が、区間と分類群ごとの誤りから計算し直した値と一致。「強いほうの対照」は物差しごとに macro F1 が高いほう |
| T-271 | G-09 / G-08 | Stage B の CNN にも学習系の陽性対照と予算の決め方を当てる | **(loop_011 で引き直した)** ラベル置換の macro F1 が、**同じ予測を fold 内で test 画像どうし入れ替えた分布の上側 97.5% を超えない**(Stage A・B とも)。予算は `ruler_a_val` で決めている。**旧基準「多数派の区間と重なる」では Stage B が 0.4963 [0.4702, 0.5212] 対 0.4119 [0.3979, 0.4250] で落ちた**(下の記録) |
| T-273 | G-09 | 入れ替え分布による偶然水準が、漏れを見分ける | 合成で、ラベルと無関係な予測は上側 97.5% を超えず、ラベルをそのまま写した予測(漏れ)は超える |
| T-272 | README の主張 | **Gram × 形の 4 セルの誤りの集計を、fold の予測から機械で出す**(事後の分析で、合否ではない) | 合成の予測で、セルごとの枚数・誤り数・分類群数が手計算と一致する。4 セルの枚数の和が全体と一致する |

T-272 を置いた理由: 物差し B・C の途中結果を手元の一回きりのコマンドで数えたところ、Gram 陰性 × 球菌だけで誤り 92%(B)/ 98%(C)と崩れていた。README に書く数は、書き捨ての集計ではなく再生成できる形で出す。**事後に見つけた偏りなので、§3.8 の判定には入れない。**

**T-271 を引き直した記録(loop_011)。** 測る前に書いた基準は「ラベル置換の区間が多数派クラスの区間と重なる」だった。
Stage B の結果はラベル置換 0.4963 [0.4702, 0.5212]・多数派 0.4119 [0.3979, 0.4250] で、**この基準では落ちた**。
漏れを疑って、置換モデルの予測を fold 内で test 画像どうし 2,000 回入れ替えた分布を作ると、
中央 0.4963・95% [0.4743, 0.5196]・観測以上の割合 p=0.544 で、**画像から何も読んでいなかった**。
落ちた原因は比べる相手にある。多数派は全部を同じ答えにする予測の値で、ばらけて答える予測の偶然水準(約 0.5)ではない。
Stage A の T-206 が通っていたのは、置換モデルが入れ替え分布より低かった(0.4356・p=1.0)からで、漏れを見分けて通ったのではない。
**基準を後から変えたので、変えたこと自体と旧基準での結果をここに残す。** 新しい基準は、同じ予測を入れ替えた分布の上側 97.5% を超えないこと(片側)。

### Stage B を画面に出す(loop_012・`tests/test_onnx_shape.py` / `tests/bundle.test.ts` / `scripts/verify-browser.mjs`)

**実装より先に書いた。** 期待値の出所は SPEC §3.11 と、loop_011 の報告(`reports/cnn_shape*.json`・`permutation_null.json`)。

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-274 | G-01 / N-01 | 形のモデルの二実装照合と入出力の形 | 全 1,842 枚で argmax 一致率 1.0・確率の最大差 < 1e-3。入力 `input` [?, 3, 224, 224]・出力 `logits` [?, 2]。30 MB 以下 |
| T-275 | G-08 | 形の出荷モデルの test は一度だけ、train だけで学習 | `test_evaluations` = 1・`trained_on` = train。ONNX で測り直した test macro F1 が記録と一致 |
| T-276 | SPEC §3.11 | 形の配信メタデータが、崩れを隠さず載せている | 三物差しと形の規則が揃う。`negative_cocci` の B・C の誤り率が `cnn_shape_cells.json` と一致し、注意書きに C の誤り率(整数 %)が入る。偶然水準と判定が載る。ラベルの並びが `["coccus", "bacillus"]` |
| T-277 | N-01 | 形の配信物が出荷物にあり、二つの ONNX の合計が 30 MB 以内 | `models/model_shape.onnx`・`labels_shape.json`・`model_shape_metadata.json` がある |
| T-278 | SPEC §3.11 / T-255 | 三つの物差しのページの Stage B の数が配信メタデータから来ている | 形の三物差しの macro F1 と陰性球菌の C の誤り率が生成 HTML に現れる |
| T-279 | G-02 / SPEC §3.11 | 実ブラウザで、形の% が Python の ONNX Runtime と一致し、注意書きが出ている | 10 サンプルすべてで形の% の差 ≤ 0.05 ポイント。結果ごとに `data-caveat="negative-cocci"` が見え、メタデータの C の誤り率が入っている |
| T-280 | G-08 / 画面の誠実さ | **画面のサンプルは、出荷モデルが学習にも選定にも使っていない画像だけ** | 全サンプルの image_id が物差し A の test に属する(train にも val にも無い)。同じ group から二枚選んでいない |

T-280 を置いた理由(loop_012): 形の出荷モデルを足すときにサンプルの分割を数えたら、**10 枚のうち 5 枚が物差し A の train、1 枚が val だった**(loop_006 から)。
画面は「うまくいく例だけを並べていません」と言いながら、半分はモデルが学習で見た画像への答えを出していた。選び方が分割を見ていなかった。

### 陰性球菌で崩れる理由(loop_013・`tests/test_mechanism.py`)

**実装より先に書いた。** 期待値の出所は SPEC §3.12 と、色相の定義(HSV の色相を回しても彩度と明度は変わらない)。

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-281 | §3.12 M3 | 偽の塗り替え(Δ = 0)は画像をほぼ変えない | 合成画像と実画像で uint8 の最大差 ≤ 1 |
| T-282 | §3.12 | 塗り替えは色相だけを動かす | 合成の桃の塊を紫へ回すと、彩度・明度の差 ≤ 2/255、染まった画素の色相が目標の ±3 度に入る |
| T-283 | §3.12 M2 | 塗り替えは形の規則の特徴量を変えない | 合成画像で細長さ・成分数が一致。素材があれば陰性球菌 126 枚すべてで一致 |
| T-284 | §3.12 | 円周平均が 0 度をまたいでも正しい | 350 度と 10 度の平均が 0 度(180 度にならない) |
| T-285 | §3.12 / G-08 | 学習し直したモデルは、抜いた陰性球菌の分類群を一枚も見ていない | 保存した train の画像に抜いた分類群が無く、test は元の fold の test と一致 |
| T-286 | §3.12 | 判定が、測る前に書いた規則から機械で出ている | 報告の数から計算し直した判定(色が主因 / 見た目が主因 / 混合 / 判定しない)が記録と一致 |
| T-287 | §3.12 | 下がり幅の区間は group 単位で、点推定を含む | 合成で、区間が点推定を含み、group を粗くすると区間が広がる |

### 色相以外の手がかりを絞る(loop_014・`tests/test_probe.py`)

**実装より先に書いた。** 期待値の出所は SPEC §3.14 と、拡大・彩度操作の定義(面積は s² 倍・細長さは不変・色相は不変)。

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-288 | §3.14 | 拡大は細長さを変えず、成分の面積を s² 倍にする | 合成の円と楕円で、細長さの差 < 0.05・面積比が s² の ±15% に入る |
| T-289 | §3.14 | s = 1.0 の偽の拡大はほぼ恒等 | uint8 の最大差 ≤ 1 |
| T-290 | §3.14 | 彩度の操作は色相を変えない | 染まった画素の色相の差 < 0.5 度、彩度の中央値が目標の ±0.02 |
| T-291 | §3.14 | 判定が、測る前に書いた規則から機械で出ている | 報告の数から計算し直した判定(大きさが主因 / 濃さが主因 / どちらでもない / 混合 / 判定しない)が記録と一致し、副作用の条件も再現する |

### 拡大の二つの効果を分ける(loop_015・`tests/test_separate.py`)

**実装より先に書いた。** 期待値の出所は SPEC §3.16 と、二つの操作の定義。
loop_013/014 で検査側の前提を 4 度誤ったので、**合成画像は塊を縁から離し、許容差は相対で置く**。

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-292 | §3.16 | 数だけの操作は、窓の中を等倍のまま残し、外を背景色で埋める | 合成で、**実装と独立に組み立てた期待画像**(窓の中だけ残し外を背景色)と画素単位で一致。丸ごと残った塊の数が、中心の座標から幾何で数えた数と一致。塊の数を「1/s² の ±30%」で見る書き方は、窓の境で切れた塊を数えて外れるので採らない(走らせる前の点検で見つけた) |
| T-293 | §3.16 | 大きさだけの操作は、塊の数を保ち、面積を s² 倍にする | 十分に離した合成の塊で、数が同じ・面積比が s² の ±15%・細長さの相対差 < 5% |
| T-294 | §3.16 P1 | 大きさだけの操作を s = 1.0 で通すと、平らな背景の合成画像はほぼ変わらない | uint8 の最大差 ≤ 1 |
| T-295 | §3.16 | 判定が、測る前に書いた規則から機械で出ている | 報告の数から計算し直した判定(大きさが効いている / 数が効いている / 両方 / どちらでもない / 判定しない)が記録と一致 |

### 背景の効果を確かめる(loop_016・`tests/test_background.py`)

**実装より先に書いた。** 期待値の出所は SPEC §3.18 と、三つの操作の定義。期待値は**実装と別の道筋で**組み立てる
(縁はチェビシェフ距離の変換で、並べ替えは値の多重集合の一致で見る)。

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-296 | §3.18 | 「平ら」は染まった画素を変えず、染まっていない画素をすべて背景色の中央値にする | むらのある背景の合成で、染まった画素が完全一致・染まっていない画素がすべて同じ値 |
| T-297 | §3.18 | 「縁を残して平ら」は、染まった画素からチェビシェフ距離 3 以内をそのまま残す | `distance_transform_cdt(chessboard)` で独立に求めた縁の画素が完全一致し、その外がすべて同じ値 |
| T-298 | §3.18 | 「並べ替え」は染まった画素を変えず、染まっていない画素の値の多重集合を保ち、並びを壊す | 値を並べた配列が一致。滑らかな勾配の背景で、隣どうしの差の平均が大きくなる |
| T-299 | §3.18 | 「並べ替え」は同じ画像 ID なら同じ結果、違う ID なら違う結果 | 二回の結果が完全一致、ID を変えると一致しない |
| T-300 | §3.18 | 判定が、測る前に書いた規則から機械で出ている | 報告の数から計算し直した判定(再現・場所・性質)が記録と一致し、副作用の確認も再現する |

### 後続ループで置くケース(現時点では未実装)

| ID | 対応要求 | ケース |
|---|---|---|
| T-209 | G-08 | test セットの評価が 1 回だけであることが手順とログから確認できる |
